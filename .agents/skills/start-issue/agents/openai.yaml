interface:
  display_name: "Start Issue"
  short_description: "Start approved MAR implementation work"
  default_prompt: "Use $start-issue to begin work on an approved MAR issue."
policy:
  allow_implicit_invocation: false
