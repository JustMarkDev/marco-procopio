interface:
  display_name: "Fix CI"
  short_description: "Repair failed GitHub Actions checks"
  default_prompt: "Use $fix-ci to diagnose and repair the failed CI run."
policy:
  allow_implicit_invocation: false
